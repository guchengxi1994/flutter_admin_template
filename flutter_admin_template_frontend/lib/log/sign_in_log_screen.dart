import 'package:flutter/material.dart';

class SignInLogScreen extends StatelessWidget {
  const SignInLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("sign in");
  }
}
