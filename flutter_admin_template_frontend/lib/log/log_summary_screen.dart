import 'package:flutter/material.dart';

class LogSummaryScreen extends StatelessWidget {
  const LogSummaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("logs");
  }
}
